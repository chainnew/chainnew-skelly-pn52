[package]
name = "hyper-slate-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
default = ["std"]
std = ["serde/std"]

[dependencies]
serde = { workspace = true, default-features = false, features = ["derive"] }
